type PlanOfStudy {
	id: String
	studentID: String
}

input NewPlan {
	studentID: String!
}

input UpdatePlan {
	id: ID!
	studentID: String!
}

type Query {
	plan(student: ID!): PlanOfStudy
	plans: [PlanOfStudy!]
}
